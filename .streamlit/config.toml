# Text-only RAG: disable Streamlit's import walker. Otherwise it may touch
# transformers vision stubs (zoedepth) and error with "No module named torchvision".
[server]
fileWatcherType = "none"

[runner]
fastReruns = true
magicEnabled = false

[client]
toolbarMode = "minimal"
